version: "2"
services:
  node:
    build:
      context: ./
    user: "node"
    working_dir: /home/node/app
    environment:
      - NODE_ENV=production
      - PORT=8080
    ports:
      - "8080::8080"
    volumes:
      - ./:/home/node/app
    expose:
      - "8888"
    command: "npm start"

